import { RoutePlaceholder } from "@/components/route-placeholder";

export default function LoginPage() {
  return (
    <RoutePlaceholder
      title="Login"
      summary="Email OTP / password flows will be implemented in the auth vertical slice."
    />
  );
}
