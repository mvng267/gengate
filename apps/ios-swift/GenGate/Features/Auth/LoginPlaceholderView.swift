import SwiftUI

struct LoginPlaceholderView: View {
    var body: some View {
        FeaturePlaceholderView(
            title: "Login",
            summary: "Auth/session UI shell for OTP and device sessions will be implemented in next batches."
        )
        .navigationTitle("Login")
    }
}
